// IIFE : Immediately Invokable Function Expression

(function name(params) { // self invoking and mostly used in module parameters
    console.log(params)
}("Fast Recovering"))

// name("Recovering") -> cannot be called again, because its scope has ended

